# Targets: YDB storage (static) nodes — monitoring HTTP ports.
# Replace hostnames/ports with your cluster values.
- labels:
    container: ydb-static
  targets:
  - "ydb-s1.example.com:8765"
  - "ydb-s2.example.com:8765"
  - "ydb-s3.example.com:8765"
